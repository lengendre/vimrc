# vimrc
```
  sudo apt update
  sudo apt install vim 
  cd ~ 
  git clone https://github.com/lengendre/vimrc.git 
  mv vimrc .vim 
  ln -s  ./.vim/.vimrc .vimrc
  Reload .vimrc and :PlugInstall to install plugins or use :PlugUpdate to update the Plugins.
```
